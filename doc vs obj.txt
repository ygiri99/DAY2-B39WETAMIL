 DOCUMENT OBJECT                                         WINDOW OBJECT  

1.It represents any HTML document or           1.It represents a browser window or frame that
web page.                                        that displays the contents of the webpage.

2.It is loaded inside the window.              2.It is the very first object that is 
                                                 loaded in the browser.

3.It is the objecty of window property.        3.It is the object of the browser.

4.All the tags,elements with attributes        4.Global objects, functions, and variables
  in HTML are part of the documents.             of javascript are memberes of the window object.

5.We can access the document from a            5.We can access the window from the window only.
  window using the window.document .              (i.e)window.window .  

6.The document is part of BOM(browser           6.The window is part of BOM only.
   object model) and DOM(document object           (not DOM).
   model).